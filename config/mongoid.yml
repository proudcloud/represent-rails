development:
  sessions:
    default:
      database: represent_ph_development
      hosts:
        - localhost:27017
      options:
        consistency: :strong
test:
  sessions:
    default:
      database: represent_ph_test
      hosts:
        - localhost:27017
      options:
        consistency: :strong

production:
  sessions:
    default:
      database: represent_ph_production
      hosts:
        - localhost
      options:
        consistency: strong
